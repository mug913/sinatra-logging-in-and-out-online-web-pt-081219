<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
<% if Helpers.is_logged_in?(session) %>
    <h1>Welcome <%=@user.username%></h1>
    <h3>Your Balance: <%=@user.balance%>.0</h3>
    <form type='POST' action='/logout'>
    <input type='submit' id='submit' value='Log Out'>
    </form>
<%else%>
  <a href="/">Log In</a>
<%end%>

  </body>
</html>
